import pygame, random, sys
from pygame.locals import *


def Ai():
    return None

def Render():
    return None

def Init():
    pygame.init()
    pygame.display.set_caption('P!ng')
    pygame.mouse.set_visible(False)
   
Init()

Score = [0,0,0,0]
PlayerPosition= [50,50,50,50]
Width = 1280
Height = 720
GamePlaying = True
mainClock = pygame.time.Clock()
windowSurface = pygame.display.set_mode((Width, Height))
 

while GamePlaying:
      or event in pygame.event.get():
        if event.type == QUIT:
            GamePlaying = False

pygame.quit()
sys.exit()
